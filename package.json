{
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "mysql": "^2.18.1"
  },
  "scripts": {
    "start": "node server/app.js",
    "build": "cd crmweb && npm install && npm run build"
  },
  "engines": {
  "node": "16.16.0"
}
}
